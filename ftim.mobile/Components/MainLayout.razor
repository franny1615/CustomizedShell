@inherits LayoutComponentBase
@inject NavigationManager Navigation

@Body

@code {
    protected override void OnInitialized()
    {
        base.OnInitialized();
        if (string.IsNullOrEmpty(KeyUtil.CurrentAuth))
        {
            Navigation.NavigateTo("/login");
        }
    }
}
